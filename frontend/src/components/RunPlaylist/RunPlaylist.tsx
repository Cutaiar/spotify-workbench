import * as React from "react";
import { StravaTest } from "../StravaTest/StravaTest";

export const RunPlaylist: React.FunctionComponent<any> = (props) => {
  return (
    <>
      <h2 style={{ color: "white" }}>Run playlist are coming :)</h2>
      <StravaTest />
    </>
  );
};
